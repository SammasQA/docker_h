
services:
  db:
    image: postgres:13-alpine #18-alpine
    environment:
      POSTGRES_DB: database_home
      POSTGRES_USER: username
      POSTGRES_PASSWORD: password
    ports:
      - '5432:5432'
    volumes:
      - ./data:/var/lib/postgresql